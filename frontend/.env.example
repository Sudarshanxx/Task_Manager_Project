# For local dev, leave blank (Vite proxy handles /api -> localhost:5000)
VITE_API_URL=

# For production (set to your Railway backend URL if deploying frontend separately)
# VITE_API_URL=https://your-app.railway.app
